/**
 * Public-facing form styles (used on /movies and similar pages).
 * Keeps visual consistency and avoids duplicating long Tailwind class strings.
 */
export const publicSelectClass =
  "h-11 w-full appearance-none rounded-2xl border border-white/10 bg-zinc-950/40 px-4 text-sm text-white shadow-[inset_0_1px_0_rgba(255,255,255,0.06)] focus-visible:bg-zinc-900/60 focus-visible:ring-primary/20";

